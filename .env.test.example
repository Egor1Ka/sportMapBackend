# MongoDB connection for billing integration tests
# The database name in the URI is used as-is (e.g. billing_test)
# Do not share this database with other processes — tests call deleteMany on all collections
TEST_MONGODB_URI=mongodb://localhost:27017/billing_test
